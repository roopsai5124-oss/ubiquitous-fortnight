{
  "name": "sheet-automation",
  "version": "2.0.0",
  "description": "Unified monthly automation for Material Costs and Labor Costs tabs — headless browser + direct API calls, writes to Google Sheets via API",
  "main": "unified-scrape.js",
  "scripts": {
    "start": "node unified-scrape.js",
    "postinstall": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "playwright": "^1.45.0",
    "googleapis": "^140.0.0",
    "xlsx": "^0.18.5"
  }
}
